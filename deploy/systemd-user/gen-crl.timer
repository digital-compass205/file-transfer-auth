[Unit]
Description=Regenerate the internal CA CRL every 6 hours

[Timer]
# Well inside the CRL's own one-day nextUpdate, so a single missed run never
# reaches the point where the services start refusing clients.
OnBootSec=5min
OnUnitActiveSec=6h
Persistent=true

[Install]
WantedBy=timers.target
