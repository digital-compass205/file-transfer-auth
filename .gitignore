# Build output -- regenerate with ./build.sh
build/

# Claude Code session state -- not part of the project
.claude/

# Local build/runtime overrides -- see README "Using a non-default Java"
java.env

# Local runtime artifacts, in case anything is run from the repo root
*.p12
*.jsonl
*.jsonl.[0-9]
ca-root.pem
agent.properties
ca-service.properties
receiver.properties
tokens.tsv
tokens.tsv.lock
